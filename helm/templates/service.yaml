apiVersion: v1
kind: Service
metadata:
  name: {{ include "mira.name" . }}
  labels:
    {{- include "mira.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mira.selectorLabels" . | nindent 4 }}
